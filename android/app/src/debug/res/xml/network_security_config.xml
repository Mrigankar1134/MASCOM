<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds may point at `npm run dev` over plain HTTP (emulator 10.0.2.2 or a LAN IP). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
